<!DOCTYPE html>
<html>
  <head>
    <title>FromaRailsApp20240204</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%# モーダルブラウザバックに対応 %>
    <%# NOTE: https://blog.to-ko-s.com/tubo-bootstrap-caution/ %>
    <meta name="turbo-cache-control" content="no-cache">
  </head>
  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <%# ロゴ %>
          <%= link_to("ルート", root_path, class: "navbar-brand") %>
          <div class="collapse navbar-collapse">
            <ul class="navbar-nav mr-auto">
            <%# パワプロ %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                パワプロ
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <%= link_to("作成選手一覧", pawapuro_index_path, class: "dropdown-item") %>
                <%= link_to("新規選手作成", new_pawapuro_path, class: "dropdown-item") %>
              </div>
            </li>
            <%# エメラルド：バトルフロンティア %>
            <li>
              <%= link_to("エメラルド：バトルフロンティア", "#", class: "nav-link") %>
            </li>
            <% if user_signed_in? %>
              <%# ログイン時 %>
              <div class="btn btn-success">
                <%= link_to "MyPage", mypage_user_path(current_user.id), class: "text-white" %>
              </div>
              <div class="text-white">
                <%= current_user.fullname %>
              </div>
              <div class="btn btn-danger">
                <%= link_to "logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-white" %>
              </div>
            <% else %>
              <%# ログアウト時 %>
              <div class="btn btn-primary">
              <%= link_to "sign up", new_user_registration_path, class: "text-white" %>
              </div>
              <div class="text-white">OLiVES</div>
              <div class="btn btn-success">
                <%= link_to "login", new_user_session_path, class: "text-white" %>
              </div>
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <%# flashメッセージ %>
    <% if notice %>
      <div class="alert alert-info alert-dismissible fade show" role="alert">
        <%= notice %>
        <button type="button" class="close" data-dismiss="alert" aria-label="dismiss">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    <% end %>
    <% if alert %>
      <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <%= alert %>
        <button type="button" class="close" data-dismiss="alert" aria-label="dismiss">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    <% end %>
    <%# 各画面 %>
    <div class="container">
      <%= yield %>
      <%= turbo_frame_tag "pawapuro_details_modal" %>
    </div>
  </body>
</html>
